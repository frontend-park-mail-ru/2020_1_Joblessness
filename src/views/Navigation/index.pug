include user
include org
include unauth
div.nav-bar--icon
    i.fas.fa-compass.fa-2x
div.nav-bar--body
    div.flex-3.flex
    +link(href="/search").nav-bar--logo
    div.flex-1.flex
    if(locals.user.role === 'PERSON')
        +user
    else if(locals.user.role === 'ORGANIZATION')
        +organization
    else
        +unauthenticated
    div.flex-4.flex
    div.nav-bar--back.nav-bar--item#nav_bar_back
        div.nav-bar--content назад