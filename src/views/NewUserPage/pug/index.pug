include bio
include sections
section.page-holder
    section.page-solid-section#users_info
    +sections.cover#users_nav
        //+sections-item.solid(name="Статистика" id=locals.events.showStatistics.id)
        //+sections-item.solid(name="Резюме" id=locals.events.showSummaries.id)
        //+sections-item.solid(name="Избранное" id=locals.events.showFavourite.id)
        //+sections-item.solid(name="Личные данные" id=locals.events.showSettings.id)
    div.width100.cover.gradient-background.flex-1
        div.width100.light-background.flex-1
            div.cover#users_current_section